version: '3.8'

services:
  ktor-letter-pet:
    build: .
    ports:
      - "8080:8080"
    environment:
      MONGO_URI: "mongodb://mongo:27017"
    depends_on:
      - mongo

  mongo:
    image: mongo:6.0
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
